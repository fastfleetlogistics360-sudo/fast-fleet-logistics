"use client";
import { useEffect, useState } from "react"; import { useSearchParams } from "next/navigation"; import { Card } from "@/components/ui/card"; import { LinkButton } from "@/components/ui/button";
export default function StorageCallback() { const search = useSearchParams(); const [message, setMessage] = useState("Confirming your storage payment…"); const [done, setDone] = useState(false); useEffect(() => { const reference=search.get("reference"); if (!reference) return setMessage("Missing payment reference."); fetch(`/api/storage-facility/verify?reference=${encodeURIComponent(reference)}&booking=${encodeURIComponent(search.get("booking") || "")}`).then(async r=>({ok:r.ok,status:r.status,data:await r.json()})).then(({ok,status,data})=>{if(status===202) setMessage(data.message); else if(ok){setDone(true);setMessage("Storage Booking Confirmed. You can track it from your bookings.");}else setMessage(data.error||"Payment needs attention.");}).catch(()=>setMessage("Payment verification could not be completed.")); },[search]); return <section className="section-wrap grid min-h-[60vh] place-items-center"><Card className="max-w-lg p-7 text-center"><h1 className="text-3xl font-black text-fleet-night">{done ? "Storage Booking Confirmed" : "Storage payment"}</h1><p className="mt-3 text-sm font-semibold text-slate-600">{message}</p><div className="mt-6 flex justify-center gap-2"><LinkButton href="/storage-facility">Storage bookings</LinkButton><LinkButton href="/hub" variant="secondary">Hub</LinkButton></div></Card></section>; }
